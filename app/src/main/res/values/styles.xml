<resources>

    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/home_title_bg</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppThemeNoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/base_red</item>
        <item name="colorPrimaryDark">@color/base_red</item>
        <item name="colorAccent">@color/base_red</item>
    </style>

    <style name="CustomTab" parent="Widget.Design.TabLayout">
        <item name="tabTextColor">@color/textcolor</item>
        <item name="tabSelectedTextColor">@color/home_tab_textcolor</item>
        <item name="tabBackground">@color/home_tab_bg</item>
        <item name="tabIndicatorHeight">0dp</item>
        <item name="android:textSize">12dp</item>
    </style>

    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>
        <!-- 指定显示的动画xml -->

        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>
</resources>
